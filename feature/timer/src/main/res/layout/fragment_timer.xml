<!-- fragment_timer.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#C51212">

    <!-- TabLayout for Pomodoro, Short Break, Long Break -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabLayoutTimer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:background="@color/colorPrimary"
        app:tabIndicatorColor="@android:color/transparent"
        app:tabIndicatorHeight="0dp"
        app:tabSelectedTextColor="@color/colorWhite"
        app:tabTextColor="@color/colorPrimaryRed"
        app:tabPaddingStart="0dp"
        app:tabPaddingEnd="0dp"
        app:tabMinWidth="0dp"
        app:tabBackground="@drawable/custom_tab_background"
        app:tabRippleColor="@color/colorAccent"/>

    <!-- ViewPager2 to enable swiping between tabs (optional) -->
    <!-- If the content is the same and only the timer duration changes, you might not need ViewPager2 -->
    <!-- You can handle tab selection without ViewPager2 -->

    <!-- Timer Text -->
    <TextView
        android:id="@+id/textViewTimer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="25:00"
        android:textSize="48sp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="32dp" />

    <!-- Start/Pause Button -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/buttonStartPause"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Start"
        android:textColor="#FFFFFF"
        android:backgroundTint="#FF6347"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="16dp"
        android:padding="12dp"
        android:background="@drawable/button_background" />

    <!-- Reset Button -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/buttonReset"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Reset"
        android:textColor="#FFFFFF"
        android:backgroundTint="#009688"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="8dp"
        android:padding="12dp"
        android:background="@drawable/button_background" />

</LinearLayout>
